# Gemini Observation Contract
Production Gemini/ADK must emit the same typed observation contract used by the application.
Model output is untrusted input and must be validated before domain-state mutation.
Gemini does not set diagnoses or safety policy.
